# =============================================================================
# Environment Configuration (Credentials & Paths)
# =============================================================================
# Copy this file to .env and customize with your actual values
# The .env file is excluded from git for security

# =============================================================================
# NASA CDDIS FTP Authentication (Optional)
# =============================================================================
# Register at: https://urs.earthdata.nasa.gov/users/new
#
# If set: Enables NASA CDDIS as primary FTP server with ESA as fallback
# If not set: Uses ESA FTP server exclusively (no authentication required)
#
# ESA server: ftp://gssc.esa.int/gnss (no auth, always available)
# NASA server: ftp://gdc.cddis.eosdis.nasa.gov (requires registration)
CDDIS_MAIL=your.email@example.com

# =============================================================================
# GNSS Data Root Directory
# =============================================================================
# Root directory where raw GNSS data is stored
# This is where RINEX files and other observation data will be located
GNSS_ROOT_DIR=/path/to/your/gnss/data

# =============================================================================
# Usage Notes
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your actual values:
#    - CDDIS_MAIL: Your registered email (or leave blank for ESA-only)
#    - GNSS_ROOT_DIR: Absolute path to your GNSS data directory
#
# 3. The .env file is automatically loaded by canvodpy
#
# 4. Other settings (processing parameters, compression, etc.) are in:
#    config/processing.yaml
#
# =============================================================================
